[project]
name = "stargazer-radar"
version = "0.1.0"
description = "Continuously updated consumer AI market map for a venture fund"
requires-python = ">=3.10"
dependencies = [
    "feedparser>=6.0",
    "python-dotenv>=1.2.2",
]

[tool.setuptools]
packages = ["radar"]
